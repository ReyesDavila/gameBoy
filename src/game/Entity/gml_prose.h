#ifndef GML_PROSE_H
#define GML_PROSE_H

// Capa de traducción GML -> C
#define x (self->base.x)
#define y (self->base.y)
#define width (self->base.width)
#define height (self->base.height)
#define solid (self->base.solid)
#define active (self->base.active)

#endif